<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cl.flores.catholicprayers"
    android:installLocation="preferExternal"
    android:versionCode="1"
    android:versionName="@string/version" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:persistent="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name="cl.flores.catholicprayers.MainActivity"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="cl.flores.catholicprayers.ViewPrayers"
            android:label="@string/title_activity_view_prayers" >
        </activity>
        <activity
            android:name="cl.flores.catholicprayers.About"
            android:label="@string/title_activity_about" >
        </activity>
        <activity
            android:name="cl.flores.catholicprayers.Preferences"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="cl.flores.catholicprayers.AddPrayer"
            android:label="@string/title_activity_add_prayer" >
        </activity>
        <activity
            android:name="cl.flores.catholicprayers.LoadActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>